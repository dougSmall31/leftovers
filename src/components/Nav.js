import React from "react";

const Nav = () => {
  return (
    <div>
      <button>Home</button>
      <button>New</button>
      <button>Favorites</button>
      <button>Logout</button>
    </div>
  );
};

export default Nav;
